%h2= @article.title
%p= markdown(@article.content)
%p= link_to("#{@category.name}", "/articles?category=#{@category.name}")

- if user_signed_in?
  .btn-group
    = link_to "Edit", edit_article_path, class: "btn btn-default"
    -if current_user.id == @article.user_id
      = link_to "Delete", article_path(@article), method: :delete, class: "btn btn-danger"
